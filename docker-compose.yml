version: '3.8'
services:
  api:
    build: .
    ports:
      - "8080:8080"
    restart: always
    environment:
      JAVA_OPTS: "-Xms512m -Xmx512m"
    
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - api
